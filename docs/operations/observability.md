# 可观测性

Actuator 当前暴露 `health` 和 `info`。生产环境应通过管理网络、网关或独立管理端口限制访问。

建议观察：

- 部门及成员关系写入成功/失败数量。
- IdP 同步模式、创建/更新/停用部门数、创建/删除关系数和耗时；日志不得包含完整人员数据。
- 策略解析延迟、空策略和拒绝次数。
- 缓存失效事件发布失败、重试和积压。
- 数据库、Nacos、Redis、服务注册和线程池健康。

日志应携带可追踪标识、机构标识的安全表达和错误码，不记录 Token、密码或第三方原始响应。
