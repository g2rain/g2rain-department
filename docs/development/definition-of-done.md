# 完成定义

- [ ] 需求目标、非目标和验收条件明确且已满足。
- [ ] 模块依赖保持 `startup → biz → api`，Controller、Service、DAO 和 DTO 未越层。
- [ ] 新增同步写契约已登记调用方、权限、事务、幂等、兼容和回滚。
- [ ] 主体、请求和数据记录的 `organId` 一致，绕过隔离的查询具有显式保护。
- [ ] 逻辑删除唯一键语义明确，迁移和历史数据已评估。
- [ ] 日志、配置、响应和文档没有真实凭证或不必要敏感数据。
- [ ] 必要的单元及集成测试已补充，`mvn clean verify` 通过。
- [ ] API、数据库、配置、部署、架构和 README 已同步。
- [ ] Markdown 链接、项目元数据、POM、启动类和端口声明一致。
- [ ] Git Diff 不包含生成器误覆盖、调试代码、日志目录和无关修改。
