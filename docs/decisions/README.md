# 架构决策记录

本目录保存 `g2rain-department` 的长期项目级架构决策。组织级规则由中央 `g2rain` 仓库维护。

需要 ADR 的情况包括：

- 长期保留跨模块同步写入例外。
- 决定 IdP 映射删除后释放或永久占位。
- 改变部门路径、权限策略或缓存一致性模型。
- 引入新的基础设施、模块或不兼容 API。

每份 ADR 至少包含状态、背景、决策、备选方案、影响、兼容、迁移和回滚。
